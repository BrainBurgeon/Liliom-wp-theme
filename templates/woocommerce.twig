{% extends 'base.twig' %}

{% block content %}
{% if function('is_page') %}
    {% set title = function('single_post_title') %}
    {% set title_split = title|split(': ') %}
    xxxxx[{{title_split|length}}]
    {% if title_split|length > 1 %}
        <h1 class="page-title">{{ title_split[1] }}</h1>
    {% else %}
        <h1 class="page-title">{{ title }}</h1>
    {% endif %}
{% elseif function('is_archive') %}
    [x]
    <h1 class="page-title">{{ function('get_the_archive_title') }}</h1>
{% endif %}
    
    {{ function('woocommerce_content') }}
{% endblock %}